@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Check-in";
}

   
    <p>Please select items to return:</p>
    <div id="admin-buttons">
        <div class="available-items">
            <h2>Items currently in your possession</h2>
            <div class="item">
                <span>Smart Board</span>
            <button class="button-link" onclick="addItem('Smart Board')">Return to Inventory</button>
            </div>
            <br>
            <div class="item">
                <span>Laptop</span>
            <button class="button-link" onclick="addItem('Laptop')">Return to Inventory</button>
            </div>
            <br>
        <div class="item">
            <span>Glue Stick Championship trophy</span>
            <button class="button-link" onclick="addItem('Glue Stick Championship trophy')">Return to Inventory</button>
        </div>
        </div>
        <hr>
        <div class="cart">
            <h2>Return Items:</h2>
        </div>
    </div>
    
<a class="button btn-info btn-lg" href="@Url.Action("Confirmation", "Inventory")">Confirm</a>
    <script>
        function addItem(itemName) {
            let items = document.querySelectorAll('.available-items .item');
            items.forEach(item => {
                if (item.querySelector('span').textContent === itemName) {
                    item.remove();
                }
            });

            let cart = document.querySelector('.cart');
            let newItem = document.createElement('div');
            newItem.textContent = itemName;
            cart.appendChild(newItem);
        }
    </script>


